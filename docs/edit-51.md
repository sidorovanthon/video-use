# edit-51: Claude Notifications — 2026-09-14

Folder: `M:/videos/OBS/prep/2026-04/2026-04-30 - Claude Notifications [REC 2026-06-25]/edit-51/`.

User chose **v7-lift58** after a fresh five-way comparison. Face-crop raw YAVG was 54.7–59.1 across all nine retained segments, with no exposure drift requiring per-segment gamma. Source was an H.264 1080×1920@60 MKV with cached Scribe `transcript.json` and no `isolated.mp3`; video and raw AAC audio were remuxed losslessly to `source_clean.mp4`. Noise-floor calibration gave p05 = −96.96 dB, so `D = 0` and the standard audio gates remained valid.

Original 72.117 s becomes 54.802 s in nine segments. Removed the abandoned `It genuinely—` false start, kept the complete retake, tightened body pauses, and preserved the final personal note, human-script disclaimer, CTA, and sign-off continuously. The first QC pass caught two long joins (0.289 s and 0.260 s) caused by inaccurate silence edges; RMS tracks moved the clean-retake onset and the `hooks.` tail, after which all eight joins measured 0.143–0.184 s.

Deliverables: `final.mp4` (1080×1920, 60 fps) and `final.srt` (96 cues, 174 words). Captions match every retained transcript word exactly; no independent user script was supplied. Integrated loudness is −14.09 LUFS with −0.66 dBTP. Every segment onset, every join waveform/frame strip, and beginning/middle/end frames passed QC with no clipped attacks or visible exposure flashes. Evidence is stored under `edit-51/verify/`.
