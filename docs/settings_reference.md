# OBS talking-head edit settings — canonical reference

Approved settings for anticodeguy's OBS vertical talking-head captures (1080×1920@60, subject on dark navy/teal backdrop, single-take monologue). **This git-backed copy is the single source of truth as of 2026-07-13** (relocated from `M:/videos/OBS/edit-01/settings_reference.md`, which now holds a pointer here). The full append-only history of that file is preserved in this repo's git history.

## Source profile

- 1080×1920 vertical, 60 fps, h264, AAC stereo 48 kHz
- OBS desktop capture, single-take monologue with false starts/retakes
- Subject: talking head, dark navy/teal backdrop, neutral indoor lighting (slightly cool)

## Output spec

- 1080×1920 @ **60 fps** (preserved from source — render.py now preserves source fps by default; `--fps 60` forces it)
- libx264, preset `fast`, CRF 20, yuv420p
- AAC 192 kbps, 48 kHz, `+faststart`

## Color grade — decision rule (the part that matters)

**Measure first, never inherit.** For every new video — even mid-series:

1. **Measure the face-crop luma (`signalstats` YAVG) of EVERY kept segment**, not just one frame — exposure can step mid-take (edit-23: front ~107–114, back ~78–83).
2. **Build a 5-up FACE-CROP comparison sheet (~660×860) at the brightest leaned-in TALKING frame** (scan YAVG across the clip to find it): raw / v7-contrast / lift52 / lift58 / lift66. A small full-frame still — especially eyes-closed — hides blow-out (that mistake let an overexposed render reach the user on edit-19).
3. **Show the sheet and let the user pick before the full render.** Explicit user request (edit-16). Series continuity is a default, never a rule (edit-19 lesson: "очевидно лицо пересвечено").

Decision rule: **bright source (face YAVG ≳ 88) → v7-contrast; dim source → v7-lift (mid 0.60–0.70).**

Series defaults (both still get the sheet):
- **"How to Avoid Losing Context …"** (dim sources, YAVG ~72–81) → **v7-lift mid 0.66**
- **"Migrating Knowledgebase With AI"** (bright sources, YAVG ~88–114) → **v7-contrast** — Parts 3–7 landed on it every time; any mid-lift blows the forehead

### v7-contrast (grade E — approved 2026-06-10 edit-11; default for BRIGHT sources)

No brightness lift: add contrast, deepen shadows, hold the white point.

```
eq=contrast=1.08:saturation=0.97,
colorbalance=rm=0.04:gm=-0.01:bm=-0.05:rh=0.03:bh=-0.04,
curves=master='0/0 0.28/0.17 0.5/0.46 0.72/0.69 1/1'
```

Key moves: true black (0→0), shadows pulled DOWN (0.28→0.17), mids a hair down (0.5→0.46), bright-face zone pulled down (0.72→0.69) to recover forehead/cheek detail, white point held (1→1). Neutral-warm colorbalance (milder than v6) kills the teal cast without yellowing skin.
Tuning: more punch → shadows 0.30→0.14 and/or face point 0.72→0.66; warmer → nudge rm/rh up a touch. **Never lift mids on a bright source.**

### v7-lift (variant D — approved 2026-06-23 edit-16; default for DIM sources)

v7's neutral colorbalance (kills v6's yellow skin) + a lifted curve for brightness.

```
eq=contrast=1.08:saturation=0.97,
colorbalance=rm=0.04:gm=-0.01:bm=-0.05:rh=0.03:bh=-0.04,
curves=master='0/0.04 0.28/0.30 0.5/0.66 0.72/0.82 1/1'
```

Brightness lever = the mid point (0.60 ↔ 0.66 ↔ 0.70); keep the neutral colorbalance fixed.

### Per-segment exposure match (edit-23 rule)

If kept segments' face luma differs materially, pre-normalize each darker segment with `eq=gamma=G` **before** the grade chain so all faces land in one band (~85–93). Use render.py's per-range `"grade"` field in the EDL: darker ranges get `eq=gamma=G,<grade chain>`, the rest use the EDL-level grade. Edit-23 approved gammas: B3 1.08 / B4 1.42 / B5 1.33 / B6 1.32 / B7 1.30 / OUTRO 1.33. A uniform grade on drifting exposure reads as "a different grade after the cut" (user flagged the 44.98s join).

### Retired / anti-patterns

- **v6** (2026-05-13, lifted warm grade: colorbalance rm+0.05/bm−0.07/rh+0.06/bh−0.08, curve 0/0.08 0.25/0.32 0.5/0.72 0.85/0.95 1/1) — retired 2026-06-23: warm balance + heavy mid-lift reads YELLOW on skin; only historical.
- v4a (contrast 1.18, mid 0.55) and v5 (contrast 1.15, mid 0.62) — rejected as too dark (edit-01/05, twice).
- Contrast > 1.20 + shadow crush 0.15→0.10 → plasticky. Saturation ≤ 0.93 with curve lift → zombie-pale skin. Black floor at 0 on dim/lifted grades → banding.
- Mid-lift (lift58/lift66) on bright faces (YAVG ≳ 88) → blown forehead, rejected on edit-11/19/20/21/22/23.

Apply the grade per-segment during extraction (Hard Rule — never post-concat, doubles re-encode).

## Audio

**Source track.** Pre-supplied `isolated.mp3` is the norm; when it is absent the pipeline cuts from the RAW source audio by default — no ElevenLabs Voice Isolation, no ffmpeg de-noise, and no such helper exists in this repo. The consequence is not audible, it is numeric: every dB gate downstream (`silencedetect` noise, the "still speech" RMS probe, the Phase 6 onset ceiling and clip line) was tuned on an isolated track whose floor is near-digital silence. Raw OBS audio sits ~10-15 dB above that. Measure the floor F (5th-percentile RMS on `source_clean.mp4`, astats at info level) and shift every level gate by `D = F - (-55)`; durations never shift. Procedure: `/video` Phase 1b.

Two-pass loudnorm, social-media spec: `I=-14 LUFS, TP=-1 dBTP, LRA=11 LU, linear=true`. Typical source ≈ −26 LUFS → ~+12 dB gain. 30 ms `afade` in/out at every segment boundary (prevents pops). AAC 192 kbps stereo 48 kHz.

## Cuts

- Snap edges to Scribe word `start`/`end`; working pad 30–200 ms past word boundaries; speech-edge padding ~50 ms head / ~80 ms tail.
- **Trim every join tight (~0.15 s residual)** — this user flags preserved 400–600 ms breaths as defects; verify joins with `silencedetect`.
- Prefer silence gaps ≥ 400 ms as cut targets.
- **Always pre-scan for retakes** (repeated phrase openings → drop the false start, keep the clean retake) and for dead-air pauses ≥ ~1.5 s inside kept regions (split and trim; usually 1–3 per take).
- Scribe caveats: verify segment STARTs with `silencedetect` (onset tokens after long silences are degenerate); read the waveform tail for closing words (word.end drifts both ways); see the project memory for the full list.

## Subtitles

- File-only deliverable (`final.srt` from working `master.srt`), no burn-in. 2-word UPPERCASE chunks, break on punctuation.
- Generated via `build_master_srt(edl, edit_dir, out)`; output time = `word.start − seg_start + seg_offset`; membership is start-based (`seg_start ≤ word.start < seg_end`) to avoid double-captioning a word straddling a split.
- **EDL source key must match the transcript filename**: with alias `S0`, copy the transcript to `transcripts/S0.json`.
- **Always cross-check against the user's script if provided.** Known Scribe fixes on this subject: `CLOUD`→`CLAUDE`, `CO-WORK`→`COWORK`, `A-A-A`/`AA`→`AI`, tense/article slips ("run"→"ran"). Apply as plain `str.replace` on a filtered transcript COPY — never edit the cached transcript.
- Burn-in spec, if ever wanted (9:16 safe-zone): FontName=Helvetica, FontSize=18, Bold=1, PrimaryColour=&H00FFFFFF, OutlineColour=&H00000000, BorderStyle=1, Outline=2, Shadow=0, Alignment=2, MarginV=90.

## Output location (monthly prep convention)

**Every video has one folder under its recording month**:
`M:/videos/OBS/prep/YYYY-MM/<dated stem>/`. Both finished videos and the
unprocessed queue use this layout. The ENTIRE `edit-NN/` working folder lives
inside the video folder. Everything — edl.json, working video, transcripts/,
clips_graded/, verify/, master.srt, progress notes, AND deliverables
`final.mp4` + `final.srt` — sits there. The video-folder root keeps only
untouched originals (`<source>.mp4`/`.mkv`, `isolated.mp3`,
`transcript.json`) plus its `edit-NN/` folder when editing has started. EDL
`sources` paths are absolute with the monthly prep path. Numbering `edit-NN`
is global across all months, so discover the maximum recursively.

If a monthly folder contains `_STATUS_EDITED_EXTERNALLY.md`, every video in
that month is already finished in an external editor such as Adobe Premiere
Pro. Exclude the whole month from transcription and editing queues regardless
of whether its video folders contain `edit` or `edit-NN`.

Pre-supplied `isolated.mp3` + `transcript.json` in the prep folder → do NOT re-run ElevenLabs; mux clean audio over video (`-c copy`) into `source_clean.mp4` and cut from that.

## Render command

```bash
uv run helpers/render.py "<prep>/edit-NN/edl.json" \
  -o "<prep>/edit-NN/final.mp4" \
  --no-subtitles \
  --fps 60
# then: cp "<prep>/edit-NN/master.srt" "<prep>/edit-NN/final.srt"
```

SRT-only rebuild:

```python
from helpers.render import build_master_srt
build_master_srt(json.loads(open('edl.json').read()), Path('<edit>'), Path('<edit>/master.srt'))
```

## Pipeline order (do not deviate)

1. Per-segment extract with grade + 30 ms audio fades baked in
2. Lossless `-c copy` concat → `base.mp4`
3. Composite overlays (PTS-shifted) — usually none
4. Subtitles LAST in filter chain — usually skipped (file-only SRT)
5. Two-pass loudnorm → `final.mp4`

## Grade history (compact per-part log)

| Date | Edit | Video | Outcome |
|---|---|---|---|
| 2026-05-06 | edit-01 | (first) | v4a initial; rejected too dark |
| 2026-05-13 | edit-05 | — | v4a rejected twice → **v6** approved |
| 2026-06-10 | edit-11 | Losing Context | v6 + softened v6 rejected "выжженным" on bright source → **v7-contrast** approved |
| 2026-06-23 | edit-16 | Losing Context P5 | dim (YAVG≈81); v6 yellow, pure v7 too dark → **v7-lift 0.66** from 4-up sheet; v6 retired |
| 2026-06-29 | edit-19 | Migrating P3 | brighter (90–94); auto-shipped 0.66 rejected "пересвечено" → **v7-contrast** |
| 2026-06-29 | edit-20 | Migrating P4 | wrong (unchosen) grade shipped, flagged → **v7-contrast**; lift66 blows forehead |
| 2026-07-06 | edit-21 | Migrating P5 | face 92–106; 5-up sheet → **v7-contrast** (lift66 output ~125 = blown) |
| 2026-07-06 | edit-22 | Migrating P6 | face 88–96; **v7-contrast**; pre-made isolated audio muxed |
| 2026-07-06 | edit-23 | Migrating P7 finale | brightest front (~114) + mid-take exposure drift → **v7-contrast + per-segment gamma match** |
| 2026-07-13 | edit-24 | Bubble/AI (standalone) | very dim, uniform (face YAVG ~43–45, no per-seg match) → **v7-lift 0.66** from 5-up sheet; dropped 2 false starts. First pass left inter-phrase pauses in the continuous body take → user flagged → recut into 16 phrase segments, every ≥0.3s pause trimmed to ~0.15s (73.4→67.8s) |
| 2026-07-13 | edit-25 | "We keep making AI and Bubble work better together" (standalone) | dim + uniform (face raw YAVG ~66–71, contrast crop 45.5, no per-seg match). Dim→lift default expected, but user explicitly chose **v7-contrast** for the moody look after seeing full frames (crop 45.5 misleads — actual face reads fine) → dim source does NOT force lift; still show the sheet & take the explicit call. 13 segs, 4 false starts dropped. Built ends from Scribe word.end first → seg10 "human being."→"For more AI tips" join measured 0.47s (Scribe end overshot acoustic silence by ~0.38s); re-derived ALL ends from source silencedetect silence_start+0.10 → every join 0.12–0.22s. Then user flagged clipped "For" onset (Scribe word.start landed LATE at 91.78 vs real onset 91.665) → fixed P11 & P12 ("Okay", also clipped) from source silence_end−0.06; verify EVERY segment start's first-frame RMS ≤ −35 dB post-render. Then user asked to keep the whole outro whole ("…human being. For more AI tips… subscribe. Okay, bye!") — merged P10/P11/P12 into ONE continuous range 88.965–94.55, natural ~0.24–0.38s sign-off pauses preserved (user: pauses acceptable), which also structurally removes any onset-clip risk there (77.4s final, 11 segments) |
| 2026-07-13 | edit-26 | "Systems Analysis and Artificial Intelligence" (standalone AI-tips) | very dim, uniform (face YAVG ~44–46, no per-seg match). v7-contrast crushed face-crop to YAVG 22.7 (background crush, not face) → user chose **v7-lift 0.66** from 5-up sheet (same family as edit-24). 12 segments; 2 false starts truncated mid-phrase ("…I already had a **test**…" retaken as "…**text** description…"; "…and based on that…" retaken); outro kept whole (sign-off exception). Key edge lesson: silencedetect `silence_start`/`silence_end` are unreliable INSIDE quiet sibilant/nasal word tails ("processes." fired at 75.79 while word audibly runs to 76.54, RMS −24 dB = still speech) and at faint pre-onset breaths ("So" onset 43.15 but silence_end 42.95 → 0.26s over-long head → join5 overshot to ~0.35s). Fix: cross-check every silence boundary against word.end + a 40 ms RMS probe before treating it as a splittable gap or a segment edge; clamp START to word.start−0.075 when silence_end sits far from the true onset. Final: every join 0.12–0.21s, all onsets ≤ −35 dB (88.8s). **SRT miss:** shipped captions from raw Scribe with only a term spot-check; user flagged text drift vs the supplied script (notation→notations, Domain Specific→domain-specific, I already→I've already, and→in, that the→that a, by human being→by a human being) → full word-by-word reconciliation, fixed in a transcript copy + rebuilt (sidecar, no re-render). See [[feedback_srt_full_script_reconcile]] |
| 2026-07-20 | edit-27 | "I honestly didn't expect… BPMN scheme" (Systems Analysis series, same as edit-26) | dim, but NOT uniform — front P00–P02 face raw ~52–59 vs back P03–P08 ~44–48 (~10–15pt step). v7-contrast crushed face to YAVG 36 on this dim source → user chose **lift58** from 5-up sheet (series-mate edit-26 used lift, but MEASURED again — series ≠ decision). Applied **per-segment eq=gamma pre-norm** (0.947–1.125) to a common raw≈54 band → final face YAVG 51.5–55.0 across all 9 segs, no join step. 16 raw phrases → 9 kept segs; dropped 4 false starts/retakes ("connec-…", "ex-", "systems an-", "Fuck.") + a dup S1 tail; trimmed 2 internal ≥0.3s pauses (S1 "conflict, and" 0.38s; S4b "itself, the" 0.40s) to ~0.15s; outro kept whole (sign-off exception). Every join 0.107–0.188s, all onsets ≤ −40 dB, −15.0 LUFS, 68.3s. **Ad-lib vs script:** only clean take of sentence-4 back-half was a retake that added "Okay, maybe" (not in script); the script-exact take had a stutter "a- ahead us" (dropped "of"). User chose to keep "Okay, maybe" → updated `script.txt` to the shipped wording so the deterministic SRT gate stays green. SRT fixes: spatial (Scribe "special"), already-corrected (hyphen merge), wanted (Scribe "want"), IDEF0 (Scribe "IDF zero" merge) → transcript-copy override + rebuild, gate exit 0 (196/196). See [[feedback_script_txt_reflects_shipped_adlib]] |
| 2026-07-20 | edit-28 | "Keep Codex Away from Your Computer" (NEW series, Part 1) | dim + uniform (face raw YAVG ~66–69 across all 19 segs, spread <3 → NO per-seg gamma). v7-contrast measured crush to YAVG 44 on the sheet; dim→lift expected, but user explicitly chose **v7-contrast** for the moody look (same call as edit-25) → establishes this series' default = v7-contrast; still measured + showed sheet. Source was **`.mkv`** (not mp4) + pre-made isolated.mp3/transcript.json → muxed clean audio (`-c:v copy`, `-c:a aac`) into source_clean.mp4, no ElevenLabs. 16 raw phrases → 19 kept segs (some phrases split at internal ≥0.3s gaps); dropped 5 false starts/stutters ("…how to fix Windows…" trailed, "I fed it…" trailed, "Codex ha-/cleaning…/cleaning up…" ×3, "Thankfully…recycle bin or…" trailed, "It ran…" stutter) each covered by a clean retake; outro kept whole (sign-off exception). Edges all mechanical from silencedetect (START = silence_end−0.06, END = silence_start+0.10). Every join 0.10–0.18s, all onsets ≤ −26 dB (none over −25 clip line), −15.0 LUFS, 89.4s. SRT gate 268/268 after 2 transcript-copy fixes (Scribe when→then, I→I'd). **Tooling gotcha:** `silencedetect`/`astats` log at ffmpeg **info** level — an `-v error` on the command hides ALL their output (looked like "no silence found"); use `-hide_banner` without `-v error`. See [[reference_ffmpeg_filter_loglevel]] |
| 2026-07-20 | edit-29 | "Keep Codex Away from Your Computer Part 2" (same series, Part 2) | dim + uniform (face raw YAVG ~68–71 across all 13 segs, spread <3 → NO per-seg gamma). v7-contrast measured crush to YAVG 47.8; **v7-contrast** chosen again (series default, same as edit-28/edit-25 moody call); still measured + showed 5-up sheet. Source `.mkv` + pre-made isolated.mp3/transcript.json → muxed (`-c:v copy`, `-c:a aac`), no ElevenLabs. 16 raw phrases → 13 kept segs; dropped 8 false starts/retakes (reinstall→"Not a clean one" ×2 trails, "Still, in my vi-", "go through it, with it", "We checked which res-", "restored." partial, "and the…"/"recommendations you're…" outro trails) each covered by a clean retake; outro kept whole (sign-off exception). **Edge lesson (INVERSE of edit-26):** on 3 sibilant/nasal word-ends silencedetect `silence_start` fired 0.4–0.6s BEFORE Scribe's `word.end` ("safe." Scribe→18.66 vs silence 18.16; "attention." →84.70 vs 84.06; "files." →60.64 vs 60.18). I nearly overrode the silence edge toward Scribe (would have kept a 0.5s pause); a 40ms RMS probe of the disputed span read **−50 to −57 dB = true silence → silencedetect was right, Scribe overshot** → kept mechanical edges, trimmed the real pauses. RMS probe is the arbiter BOTH ways (edit-26: RMS −24 dB = still speech, don't cut; edit-29: RMS −55 dB = real gap, do cut). Every join 0.09–0.19s, all onsets ≤ −29 dB, −15.0 LUFS, 89.2s. SRT gate 279/279 after: script.txt→shipped ("Part 2"→"Part Two", "you're"→"you are", both genuinely spoken) + 1 transcript-copy fix (degenerate 0.01s Scribe token "to"→"the" toward script). See [[feedback_silencedetect_subword_edges]] |
| 2026-07-27 | edit-30 | "Keep Codex Away from Your Computer Part 3" (same series, Part 3) | dim + uniform (face raw YAVG 68.6–71.0 across all 15 segs, spread 2.4 → NO per-seg gamma). v7-contrast measured crush to YAVG 47.6; **v7-contrast** chosen again — third confirmation of the series default (edit-28/29). Source `.mkv` + pre-made isolated.mp3/transcript.json → muxed (`-c:v copy`, `-c:a aac`), no ElevenLabs. 8 packed phrases → 15 kept segs (rhetorical "First."/"Second." split off as their own segs; 0.4–0.5s enumeration pauses trimmed). Dropped 2 takes: the truncated false start "First, add a bit m--" and — the new case — a **COMPLETE duplicate** of the closing line "I wish you no BSODs." (said at 72.44–73.56 and again at 74.62–75.70, both full sentences, no truncation marker; only signal was the same line ending one packed phrase and starting the next). Kept the SECOND copy so the outro runs unbroken to "Okay, bye!" as one un-cut range (sign-off exception). Edge notes: Scribe's "Codex." (P13) overshot to 72.419 across TWO silences while the acoustic end was 71.811 (RMS −46…−60 dB = true silence) — same inverse pattern as edit-29; conversely before "So" (P01) silencedetect's `silence_end` 6.064 sat 0.135s ahead of Scribe's `word.start` 6.199 and the RMS probe read −10…−20 dB = **speech already running**, i.e. Scribe's onset was late, so the mechanical `silence_end − 0.06` start was correct. RMS probe arbitrated both directions again. All 14 joins 0.100–0.173s (the two 0.33–0.34s gaps inside the outro are the intentional sign-off pauses), all onsets ≤ −36.7 dB, −14.9 LUFS, 60/1, 67.3s from 83.1s. SRT gate 208/208 after: `script.txt` title → shipped wording ("Part 3"→"Part Three", genuinely spoken) + 3 transcript-copy fixes (Scribe sections→actions, nope→no, SOTS→BSODs). See [[feedback_complete_duplicate_retake]] |
| 2026-07-27 | edit-31 | "We continue the saga of systems analysis with AI and Claude" (Systems Analysis series, follows edit-26/edit-27) | dim + very uniform (face raw YAVG 67.7–71.0 across all segs, spread 3.3 → NO per-seg gamma). Series-mates edit-26/27 both chose **lift** (v7-contrast crushed their faces to 22.7 / 36) — but here v7-contrast measured a healthy 46.9 (dim background, well-lit face), and the user chose **v7-contrast** → the "Systems Analysis" series has NO settled grade; measure and show the sheet every time. Source `.mkv` + pre-made isolated.mp3/transcript.json → muxed (`-c:v copy`, `-c:a aac`), no ElevenLabs. 16 packed phrases → 12 kept segs; dropped 6 false starts/retakes ("For example…" ×2 trails, "…you can build u-", "…what had already been gathered was…" trail, "…usually start from." partial, "For more AI s-" / "For more stuff like this…" trails) + one **complete duplicate** of "This process starts with processing all the raw materials available at the input." (said 14.20–19.00 and again 24.06–27.86, both full sentences; kept the SECOND — same words delivered in 3.8s vs 4.8s, i.e. the retake is 26% tighter, and the swallow sat between them). Outro kept whole (sign-off exception). All edges mechanical from silencedetect (START = silence_end−0.06, END = silence_start+0.10); all 11 joins 0.078–0.162s, all onsets ≤ −34.9 dB, −15.0 LUFS, 60/1, 76.0s from 149.6s. **Miss + re-render:** at cut time I dismissed the 0.350s gap at 102.640–102.990 as a sub-word tail because Scribe's "meetings" (102.20–103.00) spanned it — pattern-matching [[feedback_silencedetect_subword_edges]] instead of probing. It was real silence (RMS −43…−56 dB, Scribe word.end overshot 0.36s); only surfaced when an unrelated SRT-gate question forced the probe → split P07 and re-rendered. Rule hardened: probe EVERY ≥0.3s gap inside a kept range, especially the ones a Scribe word spans. SRT gate 228/228 after 2 transcript-copy fixes (Scribe "there are"→"they're a", "and"→"in") and 3 **script-side** fixes — the script contained words never spoken: parenthetical gloss "(calls)", article "the objects" (0.08s of continuous speech, no room), and singular "client" where the tail carries a clear /s/ (HF-bandpass RMS −21 dB vs −25 dB "processes" control) → decide each gate mismatch by audio probe, not by assuming the script is truth. See [[feedback_script_txt_reflects_shipped_adlib]] |
| 2026-07-27 | edit-32 | "How to make AI and systems analysis work together" (Systems Analysis series, same shoot date as edit-31) | dim + very uniform (face raw YAVG 68.9–74.1 across all 14 segs, spread 5.2 → NO per-seg gamma). v7-contrast measured a healthy 46.7 (same as edit-31's 46.9) → user chose **v7-contrast** again, 2nd consecutive for this series — but edit-26/27 chose lift on the same "dim" label, so the series still has no default: measure every time. See [[feedback_grade_systems_analysis_series]]. Source `.mkv` + pre-made isolated.mp3/transcript.json → muxed (`-c:v copy`, `-c:a aac`), no ElevenLabs. 24 packed phrases → 14 kept segs; dropped 9 false starts/retakes, incl. a **4-deep retake stack** on the "command line" paragraph (attempts at 128.50, 133.94, 139.20 all truncated). Two of the stack were COMPLETE takes and the tie-break was textual, not positional: the earlier one (113.24–124.66) swallowed "it" ("how I do most of the time") and the LAST one (158.78–175.59) ended "or in your **ID**, of course" instead of CLI → kept the middle take 145.30–156.34, the only one matching the script and needing no internal splice. "Last take wins" would have shipped the wrong word. Outro kept whole (sign-off exception). All 13 joins 0.10–0.18s, all 14 onsets ≤ −39.6 dB, −14.9 LUFS, 60/1, 72.2s from 198.7s. **Tail-gate catch + re-render:** END of "But that's not it." was placed after a blip that split two adjacent silences (183.299–183.552 / 183.552–183.788), read as the /t/ release → rendered join measured **0.40s**. RMS probe: the blip is **−44.9 dB = inaudible**, real acoustic end 183.29 → END 183.650 → 183.390, re-rendered, join 0.174s. New rule: probe the blip level between two adjacent gaps; ≲ −40 dB → cut BEFORE it. See [[feedback_silencedetect_subword_edges]]. SRT gate 248/248 with **zero transcript-copy fixes** — all 4 mismatches were script-side (Scribe right every time): "all of this"→"all this" (50ms dip at −40 dB, no room for "of"), "artificial"→"our artificial" (real 130ms voiced syllable at −15 dB), "How do you avoid"→"How to avoid" (110ms of voiced material = one syllable, not two), "a terminal"→"the terminal" (all four takes transcribe "the"). See [[feedback_script_txt_reflects_shipped_adlib]] |
| 2026-08-03 | edit-33 | "When you start working with a coding agent… pick a working repository" (same 2026-05-14 shoot as edit-32) | bright-ish + very uniform (face raw YAVG 72.0–78.4 across all 10 segs, spread 6.4 → NO per-seg gamma). v7-contrast measured 49.9 — the healthiest contrast reading of the Systems-Analysis family so far → user chose **v7-contrast**, 3rd consecutive; still measured + showed the 5-up sheet. See [[feedback_grade_systems_analysis_series]]. Source `.mkv` + pre-made isolated.mp3/transcript.json → muxed (`-c copy`), no ElevenLabs. 15 packed phrases → 10 kept segs; dropped 5 false starts/retakes (3-deep stack on "But what I'm suggesting…" at 46.04 and 52.74 before the clean 63.34 take; 2 truncated "Meaning, we have a repository…" attempts at 74.75/83.86; "I'm not gonna complicate this/things" ×2 plus the "point out our…" trail at 122.56). Outro kept whole (sign-off exception). Two internal ≥0.3s gaps were each **spanned by a Scribe word** and both turned out to be real silence: `from.` (20.06–22.44) straddled a **2.28 s** gap probing −62…−70 dB, and `matter.` (107.92–108.50) straddled 0.394 s probing −44…−48 dB → split both, per the edit-31 probe-every-gap rule. All 9 joins 0.109–0.165 s, all 10 onsets ≤ −36.4 dB, −14.8 LUFS, 60/1, 75.2 s from 153.0 s. SRT gate 247/247 after 12 transcript-copy fixes, of which 3 needed **spectral arbitration**, not context: `engine`→`agent` (word tail −52…−55 dB in every band = no nasal murmur, so no /n/ coda), `run`→`ran` (F2hi−F2lo = −2.81 dB vs −3.47 for the /æ/ control "that" and −3.76/−4.05 for the /ʌ/ controls), `the`→`a` (0.010 s degenerate token; real `the` runs 0.10–0.12 s here). Rest were orthography/product-name: `Cloud Coder`→`Claude Code or`, `code base`→`codebase` ×3, `we are`→`we're` ×3 (shortest span 0.180 s — too short for two words). See [[feedback_word_identity_spectral_probe]] |
| 2026-08-03 | edit-34 | "The very first step… create a folder on your computer for your project" (same 2026-05-14 shoot as edit-32/33; new topic — project folder structure) | dim-ish + the most uniform source yet (face raw YAVG 73.3–77.7 across all 13 segs, spread **4.4** → NO per-seg gamma). v7-contrast measured 53.7 — the healthiest contrast reading of this shoot (edit-31 46.9, edit-32 46.7, edit-33 49.9) → user chose **v7-contrast**, 4th consecutive; still measured + showed the 5-up sheet. Source `.mkv` + pre-made isolated.mp3/transcript.json → muxed (`-c copy`), no ElevenLabs. 19 packed phrases → 13 kept segs; dropped 6 false starts/retakes: the abandoned first run at the whole "structuring that space" paragraph (19.28–36.82, trails off at "how we'll manage…"), the "Because it matters for both…" stub, "That is critically important… speeds up…" (superseded by the "This is…" take), the truncated "The AI doesn't need to do a contextual search." restart, a **3-deep stack** on "To do that, open…" ("open cha-" at 119.66, then a take ending "reasoning modes." that never reached "and describe the project", then the complete take at 133.80), and the stutter "If it's not, if it's not…". Outro kept whole (sign-off exception). **New silencedetect failure mode:** the 0.41 s pause at 140.435–140.853 produced **no silencedetect event at all** at −35 dB/0.20 s — only the Scribe word gap (140.48→140.90) flagged it, and the RMS probe confirmed −36…−61 dB throughout → split P10/P11. Enumerate pauses from the UNION of silencedetect gaps and Scribe word gaps, not from silencedetect alone. See [[feedback_silencedetect_subword_edges]]. All 12 joins 0.107–0.161 s, all 13 onsets ≤ −54.8 dB, −14.9 LUFS, 60/1, 75.6 s from 164.0 s. SRT gate 255/255 after 5 mismatches, every one arbitrated acoustically and splitting **3 transcript-side / 2 script-side**: Scribe wrong on `Coder`→`Code or` (2nd syllable F2 1209-1355 vs the speaker's confirmed "or" at 1118-1394), `there is`→`there's` (0.270 s span vs 0.300/0.330 s for two `there's a` controls — an extra syllable can't be shorter), and `the`→`a file` (0.060 s nucleus, below every `the` control); Scribe RIGHT on `in`→`on` (F2 ≈1170-1200 in both takes vs ≈1360 for the matched-environment control `what's IN them`) and `this`→`the structure` (frication 0.145 s vs 0.225 s for the `file structure` control — shorter, so no second /s/). Band-ratio probes were inconclusive on the reduced function words; LPC formants + matched-environment controls settled them → promoted the tooling to `helpers/word_probe.py`. See [[feedback_word_identity_spectral_probe]] |
| 2026-08-03 | edit-35 | "How can complex tasks be solved with the help of AI" (new 2026-05-21 shoot, new standalone AI-tips topic) | the DIMMEST recent source (face raw YAVG 61.9–68.4 across all 12 segs, spread 6.5, front ~67 vs tail ~63 → user declined per-seg gamma; measured max join step afterwards was 1.4 YAVG = 3%, invisible). v7-contrast measured **45.6** — the lowest reading ever shipped as contrast — yet the user chose **v7-contrast**, 5th consecutive. The decider was on the sheet, not in the number: the raw frame has a haze / lifted-black cast and every lift variant amplified it, while v7-contrast was the only one producing a clean black background. See [[feedback_grade_systems_analysis_series]]. Source `.mkv` + pre-made isolated.mp3/transcript.json → muxed (`-c copy`), no ElevenLabs. 10 packed phrases → 12 kept segs; dropped 4 truncated retakes ("this is-", "…repeat something several-", "…understood by the A-", "The most important part is that-") plus a **duplicated phrase head**: the take ending "…but with the complex tasks," (31.56) and the retake opening with the same four words (34.92). Rather than cut inside the zero-gap "approach,|but" boundary, the join was placed at the two natural pauses AROUND the duplicate — keep through "…complex tasks," (31.77) and resume at "there is often…" (36.50) — which reproduces the script exactly and lands both edges in silence. Outro kept whole (sign-off exception), but the 0.68 s lead-in gap BEFORE it was trimmed. **Both silencedetect failure modes in one file:** it missed two real gaps that only the Scribe word gaps flagged (10.48→10.79, floor −58 dB; 24.79→25.14, a −42…−57 dB breath) — RMS-probed and cut; and conversely it hid a genuine **0.84 s** pause at 103.26–104.10 *inside* Scribe's word "well." (103.06–104.08), which only silencedetect saw. Union of both sources, probe everything. A third candidate (17.42→17.58) probed at only 0.16 s → correctly left alone. All 11 joins 0.09–0.19 s, all 12 onsets ≤ −30 dB in the normalized output (≈ −40 dB source), −14.8 LUFS, 60/1, 74.2 s from 134.3 s. **New gate calibration bug:** the Phase 6 `silencedetect -35dB` pass on `final.mp4` found NO event at 3 of the 11 joins — loudnorm had already added +9.7 dB (pre-norm I = −23.75 LUFS), so the source-side threshold no longer applies to the output; at −26 dB all 11 appeared. See [[feedback_loudnorm_shifts_silence_threshold]]. SRT gate 235/235 after 8 mismatches, every one arbitrated acoustically, splitting **3 transcript-side / 5 script-side**: Scribe wrong on `abilities`→`capabilities` (five RMS peaks with voiceless closures at −40.7/−40.3 dB = 5 syllables, not 4) and the two product names (`Whisperflow`→`Wispr Flow`, `Glider`→`Glaido`); Scribe RIGHT on `does not`→`doesn't` (one −17.0 dB peak + a reduced bump 11 dB down, matching two in-file `doesn't` controls), `used`→`use` (structurally identical to the confirmed "use the" at 113.54, no /d/ closure — the weakest of the five calls and flagged as such), the extra `the` in "with the complex tasks" (a real 50 ms voiced syllable at −22.3 dB between the "with" tail and the /k/ closure), `pattern`→`patterns` (0.17 s sibilant at sib 0.98, matching the "things" /z/ control; the "project" control shows none) and `some things`→`something` (the sibilant before "several" runs 0.11 s — SHORTER than both clean single-/s/ controls at 0.12/0.16 s, so no plural /z/ is stacked on it). See [[feedback_word_identity_spectral_probe]]. **Follow-up cut (same session):** user asked to drop "Claude doesn't have speech recognition yet," so P11 reads "…out of the box, but tools like Wispr Flow…". The two boundaries were opposite cases. After "box." there is real silence (119.14→119.43, floor −55 dB) → END = 119.240 by the usual `silence_start + 0.10`. Before "but" there is **effectively none**: the "yet,"→"but" dip is 0.05 s deep and only reaches −38 dB — a /t/ closure running straight into the next word, not a pause. `silence_end − 0.06` would have started at 121.25, inside the −34 dB tail of "yet". Started at **121.270** instead — inside the closure, where nothing is audible — trading the 0.06 s lead-in down to 0.04 s. Rendered join measured 0.155 s of quiet (−47…−63 dB source-equivalent) and the P11b onset read −27.1/−35.3/−39.1 dB, i.e. no fragment of "yet" leaked. Rule: when the gap before a segment is a consonant closure rather than a pause, place START by the closure floor, not by the −0.06 offset. Re-render: 13 segs, 72.2 s, −14.8 LUFS, 60/1, SRT gate 229/229 (transcript copy also got `box.`→`box,` so the caption chunk breaks correctly mid-sentence) |
| 2026-08-10 | edit-36 | "Simple but extremely important task for you and the agent — develop a file structure for the project" (same 2026-05-21 shoot as edit-35; continues the standalone AI-tips run) | dim and the most uniform source of the whole run (face raw YAVG **65.1–68.6** across all 12 segs, spread **3.5** → NO per-seg gamma; measured max join step afterwards was 2.1 YAVG = head movement, not a grade step). v7-contrast measured **45.4**, essentially edit-35's 45.6 and the same "lowest ever shipped" territory → user chose **v7-contrast**, 6th consecutive; still measured + showed the 5-up sheet. See [[feedback_grade_systems_analysis_series]]. Source `.mkv` + pre-made isolated.mp3/transcript.json → muxed (`-c:v copy`, `-c:a aac`), no ElevenLabs. 16 packed phrases → 12 kept segs from 134.9 s of source, the heaviest drop ratio yet (63 s discarded): 4 truncated retakes ("…hierarchical nesting that's-", "…then preliminary cl-", the orphan "should…", "Or if you are…" / "Or i-…" / "…then in this…" as a **3-deep stack** on the same sentence) plus a **duplicated phrase head** — the good "A good practice…" take runs on into "Then we take the specified file structure. As a rule, LB…" and dies, while the next take opens with those same words; cut after "…explicitly stated." (64.18) and resumed at the retake (71.94), both edges landing in silence. Outro kept whole (sign-off exception; its two internal 0.30/0.34 s pauses are intentional and match edit-35). Every edge derived mechanically from RMS probes rather than Scribe: `stated.` overshot **0.62 s** (word.end 64.70 vs acoustic 64.08) and the `And` of P08 was the reverse — silencedetect's `silence_end` 84.087 AND Scribe's `word.start` 84.32 both missed the true onset at 84.24. Two sub-0.3 s candidates were probed and correctly left alone (folders→based bottoms out at only −39.7 dB for 20 ms = a /b/ closure; rule,→it'll is 0.14 s). All 11 joins **0.130–0.188 s**, all 12 onsets ≤ −40.8 dB, −14.9 LUFS, 60/1, 71.6 s. Phase 6 gate run at **−27.1 dB** on `final.mp4` per [[feedback_loudnorm_shifts_silence_threshold]] (pre-norm I = −22.80 → +7.9 dB of loudnorm gain); `final.prenorm.mp4` is deleted by render.py, so shifting the threshold is the only option. SRT gate 243/243 after **7 mismatches, all 7 transcript-side** — the first clean sweep, each still individually probed: `Cloud Caller`→`Claude Code or` (a **−45.1 dB / 50 ms stop closure** sits where "Caller"'s lateral /l/ would have to be a continuant, and the first vowel holds F1 505–568 = monophthong /ɔ/, not /aʊ/; note the run goes 2 tokens → 3, so it needed retiming plus an inserted `or`, not a text override), `engine`→`agent` (F2 1834–1850 in the frame before the /dʒ/ = the /eɪ/ offglide, vs 1332–1434 for the nasal in the "end" control), `Glider.`→`Glaido.` (F3 2367–2432 = non-rhotic, against "folders" /ər/ at 1658–1839 and "go" /oʊ/ at 2223–2429), `preliminarily`→`preliminary` (0.60 s vs **0.69 s** for the same speaker's "preliminary" in the discarded retake — an extra syllable cannot be shorter), `it will`→`it'll` (F2 stays 800–1150 for 0.14 s with no excursion to ~1800, so no /ɪ/ between the /w/ and the dark /l/), `you are`→`you're` (one continuous RMS arch, F1 peaks at 504 = no /ɑ/), and `Whisper`→`Wispr` (pure orthography, identical phonemes). Three of the seven were the recurring brand mishears — see the new [[feedback_scribe_product_name_mishears]]. See also [[feedback_word_identity_spectral_probe]] |
| 2026-08-10 | edit-37 | "Important observation when working on projects with AI — fill the project folder with context" (same 2026-05-21 shoot as edit-35/36; continues the standalone AI-tips run) | dim and uniform (face raw YAVG **66.0–69.2** across all 18 scanned segs, spread **3.2** → NO per-seg gamma; measured max join step afterwards was **1.5** YAVG across all 20 joins). v7-contrast measured **46.9** — the same reading the user accepted on edit-31 (46.9) and edit-32 (46.7) → user chose **v7-contrast**, 7th consecutive; still measured + showed the 5-up sheet. See [[feedback_grade_systems_analysis_series]]. Source `.mkv` + pre-made isolated.mp3/transcript.json → muxed (`-c copy`), no ElevenLabs. 15 packed phrases → **21 kept segs** from 139.2 s; dropped 2 retake stacks: a **3-deep** run at "For example, for transcripts…" (the trailing false start inside the 46.16 phrase, then the truncated 58.56–64.90 attempt, then the complete 67.64 take) and a 2-deep run at "And if there's no such folder…" (the orphan "And," at 109.30 and the truncated "…it will," at 111.96 before the complete 116.62 take). Outro kept whole (sign-off exception; its two internal 0.27/0.34 s pauses are intentional and match edit-35/36). **Three real pauses were missed at cut time and only caught by the Phase 6 gate** — all three sat *inside* a Scribe word span, so neither the word-gap list nor a containment filter flagged them: `presentations.` (28.64–29.76) hid 0.33 s, `structure.` (122.88–123.62) hid 0.39 s, and worst, `with.` (35.54–**36.26**) hid a **0.66 s** pause that produced no Scribe word gap at all because the word's `end` was stretched to the next word's onset. Split all three (P05→P05a/b, P06→P06a/b, P16→P16a/b) and re-rendered. `possible.` showed the same failure in the opposite direction — `word.end` 15.12 vs acoustic end **13.96**, a 1.16 s overshoot, where silencedetect was right. See [[feedback_silencedetect_subword_edges]]. **New Phase-6 tooling bug:** the onset check located joins by summing EDL durations, but per-segment re-encode rounding drifts the output timeline (~0.13 s by mid-file), so two probes landed in the *previous* segment's decaying tail and falsely read −19.4 / −24.1 dB "hot onsets". Locating the joins from `silencedetect` on `final.mp4` instead showed both clean (decay → −100 dB splice → ramp). See [[feedback_join_offsets_from_silencedetect]]. All 20 joins **0.114–0.190 s**, −14.8 LUFS, 60/1, 92.7 s from 139.2 s. Phase 6 gate run at **−26.3 dB** per [[feedback_loudnorm_shifts_silence_threshold]] (pre-norm I = −22.75 → +8.75 dB of loudnorm gain). SRT gate **303/303** after 13 mismatches, splitting **8 transcript-side / 5 script-side**: Scribe wrong on `cold`→`call` (F2 862–1060 at the nucleus vs **744–866** for the speaker's own /oʊ/ in the `folder` control — not back-rounded enough for "cold"), `AA`→`AI` ×3 (F1 peaks at **624**, above the speaker's /eɪ/ ceiling of 503 in the `away.` control, so an open nucleus is present), `you are`→`you're` (one continuous F1 arc 272→903 with no second nucleus; the region Scribe labels "are" is decaying −21→−31 dB = the /h/ of "having"), `it will`→`it'll` (F2 floor **802** vs **565** for the /w/ in the discarded "it will," retake 5 s earlier, and 0.10 s of voicing vs 0.16 s), plus `process transcripts`→`processed-transcripts` (final sibilant 0.16 s vs 0.09 s for the single /s/ in the `processed and` control — the weakest of the eight and flagged as such) and the `raw`+`transcripts` merge for the folder name; Scribe RIGHT on `into`→`in` (exactly **two** voiced blobs between "placed" and "created", 37.63–73 and 37.75–80 — "into the" needs three), `ones`→`one` (sib **0.000** at the tail, identical to the `one` control — no /z/), `just`→`should` (sibilant only at the ONSET, sib 0.001 for the final 0.2 s → no /st/ cluster, matching the `should` control), and the written gloss "(see previous posts)" → the shipped ad-lib **"watch my previous video"**. See [[feedback_word_identity_spectral_probe]] and [[feedback_script_txt_reflects_shipped_adlib]] |
| 2026-08-10 | edit-38 | "Complex Projects and AI Agents 1 on 1 — gather every artifact, then have the agent inventory and rename them" (same 2026-05-21 shoot as edit-35/36/37; continues the standalone AI-tips run) | the BRIGHTEST source of this shoot (face raw YAVG **64.1–73.6**, per-segment averages 66.2–71.9, spread **5.7** → no per-seg gamma; edit-35 declined it at spread 6.5 and the measured join step came to 1.4). v7-contrast measured **50.9** — the highest reading of the run (edit-35 45.6, edit-36 45.4, edit-37 46.9) and near edit-34's 53.7 → user chose **v7-contrast**, 8th consecutive; still measured + showed the 5-up sheet. Decider unchanged from edit-35: raw and every lift variant amplify a hazy lifted-black cast, v7-contrast is the only one giving a clean black background. See [[feedback_grade_systems_analysis_series]]. Source `.mkv` + pre-made isolated.mp3/transcript.json → muxed (`-c:v copy`, `-c:a aac`), no ElevenLabs. **22 packed phrases → 24 kept segs from 216.6 s → 100.5 s**, the heaviest discard ratio of the run (116 s dropped). Retake stacks: a **5-deep** run on the closing paragraph ("The result of this stage…" abandoned at 149.54 "with the help of the…", 167.86 "with the help of an…", 182.78 "ready for the further…", 189.58 "ready for the…", complete take at 197.00), a **3-deep** run on "For example, add the file creation date…" (truncated at 55.26, "as a prefix supply" at 58.48, orphan "For example, the file…" at 64.16, clean take at 67.60), plus truncated "develop a spec-", orphan "So…", "add missing direc-", "understand it, sh-" and a duplicated phrase head ("Suggest … surface level breakdown." said twice, 14.54 and 24.44). Outro kept whole (sign-off exception) — its internal pauses here are **0.359 s / 0.447 s**, longer than edit-35/36/37's 0.27–0.34, and the user explicitly chose to keep them. **Scribe stretched `word.end` over a pause on 6 of the 24 segment ends** — `order.` claimed to 38.72 vs acoustic 38.01 (0.71 s), `first.` to 127.96 vs 127.12 (0.84 s), `forward.` to 118.72 vs 118.20, `hard.` to 136.54 vs 136.11, `folder.` to 94.66 vs 94.34, `mean.` to 39.72 vs 39.15 — so every edge came from measurement, never from Scribe. Conversely the 0.53 s pause at 83.68–84.21 produced **no silencedetect event at all** at −35 dB/0.20 and was found only by a 10 ms RMS scan, while the 0.32 s word gap Scribe reports at `structure|and` (99.18–99.50) **does not exist acoustically** — a ground-truth RMS scan settled both directions. Three real sub-threshold gaps correctly left uncut (`are|placed` 0.251, `prefix,|apply` 0.259, `tasks,|including` 0.250). All 23 joins **0.143–0.199 s**, max face-luma step across joins **2.9 YAVG** (6 %, head movement — eyes closed→open at J8), **−15.0 LUFS** (1 LU under the run's usual −14.8/−14.9), 60/1. **Two new Phase-6 measurement bugs, both about the probe rather than the cut:** a plain PCM decode of `final.mp4` ran **+0.506 s long = 24 segs × 1024 samples of AAC priming**, drifting every probe and producing 14 false "HOT onset" flags plus impossible −10 dB silence floors — fixed by prefixing `aresample=async=1000:first_pts=0`, after which the decode matched the container to 1 ms (`source_clean.mp4` has zero drift, so Phase 3 was unaffected); and an onset probe anchored at `silence_end` is **above the threshold by definition**, which false-fired on 18 of 23 verifiably clean joins. Every join was confirmed by SHAPE instead: decay −17.9→−50, a **−91 dB splice notch**, then the next segment's room tone ramping up. See [[feedback_final_mp4_aac_priming_drift]] and [[feedback_onset_probe_anchor_artifact]]. Phase 6 silencedetect run at **−26.6 dB** per [[feedback_loudnorm_shifts_silence_threshold]] (pre-norm I = −22.58 → +8.42 dB of loudnorm gain). SRT gate **329/329** after 8 mismatches, splitting **5 transcript-side / 3 script-side**: Scribe wrong on `101`→`1 on 1` (the middle syllable is a nasal murmur, F1 203–358 matching the `-tion` control's 222–324, against /oʊ/ at 476–540 in `folder` and 531–627 in `don't`, and sitting 13 dB below both flanking vowels), `up certainly`→`absolutely` (F3 never drops below **2365 Hz**, whereas the rhotic /ɜr/ falls to 1537 in `surface` and 1323 in `further` — no "cer-" is present; stress pattern strong-weak-strong-strong also matches /ˌæb.sə.ˈluːt.li/, not "up ˈcer-tain-ly"), and `structural`→`structure` (**470 ms** from /s/ onset to /ər/ offset vs **500 ms** for the confirmed `structure` in "established file structure", with identical internal landmarks — /ʌ/ peak +240 ms, /k/ closure +270/+280, /tʃ/ +340/+330 — and an extra syllable cannot make a word shorter), plus the two hyphenation merges `surface-level` and `ready-for-further-work`; Scribe RIGHT on `What do I mean`→`What I mean` (**two** nuclei before "mean" in both takes, not three; take 2's −30.7 dB dip is the /t/ between "What" and "I") and `they're`→`there are` (F3 bottoms at **1583 Hz in the middle** of the run and recovers to 2011 by the end, so the rhotic belongs to the FIRST syllable = `there`, followed by a second nucleus at F1 ≈630; 240 ms total vs 260 ms for the `if there are a lot` control, where one-syllable "they're" would be ~150 ms). The **weakest call of the eight** was `to the`→article: the syllable count decisively kills the script's two-syllable "to the" (one nucleus vs two humps through a dip in the `suggest to the agent` control, corroborated on the discarded 14.54 take at F1 503/F2 1345 vs the kept take's 516/1342), but F2 **cannot** separate `a` from `the` for this speaker — four `a` controls span 1139–1435 and three `the` controls span 1230–1385, fully overlapping. Flagged as unresolved and the user chose `the`. See [[feedback_word_identity_spectral_probe]] and [[feedback_script_txt_reflects_shipped_adlib]] |
