# edit-53: LangGraph — 2026-09-14

Folder: `M:/videos/OBS/prep/2026-05/2026-05-02 - LangGparh [REC 2026-06-25]/edit-53/`.

User chose **v7-lift58** after a fresh five-way comparison. Face-crop raw YAVG was 56.4–62.1 across all eleven retained segments, with no exposure drift requiring per-segment gamma. Source was an H.264 1080×1920@60 MKV with cached Scribe `transcript.json` and no `isolated.mp3`; video and raw AAC audio were remuxed losslessly to `source_clean.mp4`. Noise-floor calibration gave p05 = −80.41 dB, so `D = 0` and the standard audio gates remained valid.

Original 104.967 s becomes 65.886 s in eleven segments. Removed two abandoned attempts at the video-pipeline paragraph and kept the only complete retake. Ordinary body pauses were shortened, including the real 0.49 s pause before `Old faithful Camunda` that the silence detector understated because of a pre-onset sound. The invitation, emphatic closing, human-script disclaimer, CTA, and sign-off remain continuous.

Deliverables: `final.mp4` (1080×1920, 60 fps) and `final.srt` (99 cues, 184 words). With no independent user script, captions were reconciled against the retained transcript; the obvious ASR orthography `retro docked` was corrected to `retro doc` only in the edit-local transcript copy. Integrated loudness is −14.12 LUFS with −0.77 dBTP. All ten joins measure 0.144–0.182 s, every segment onset is at or below −62.5 dBFS on `base.mp4`, and every join frame pair plus beginning/middle/end samples passed with no clipped attacks or visible exposure flashes. Evidence is stored under `edit-53/verify/`.
