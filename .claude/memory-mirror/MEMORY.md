- [User role](user_role.md) — anticodeguy: OBS vertical talking-head videos for socials, iterates closely on grade/cuts, expects end-to-end driving + memory updates
- [Feedback — grade brightness](feedback_grade_brightness.md) — "How to Avoid Losing Context" series DEFAULT (edit-16) = v7-lift (v7 neutral balance + curve lifted, mid 0.66). v6 yellows skin (rejected), pure v7-contrast too dark on dim source (rejected). v6 retired. ALWAYS show comparison frames before the full render
- [Feedback — Migrating series grade](feedback_grade_migrating_series.md) — "Migrating Knowledgebase With AI" sub-series (Parts 3–7) grades to v7-contrast (bright faces YAVG ~88–106); OPPOSITE of the v7-lift default; still measure + show the sheet
- [Feedback — per-segment exposure match](feedback_exposure_match_segments.md) — source face luma can drift between halves (edit-23 front ~114 vs back ~80); uniform v7-contrast → visible brightness step at the cut (user flagged). Per-segment pre-normalize with eq=gamma=G before the grade; render.py now has a per-range "grade" field
- [Reference — video-use settings](reference_video_use_settings.md) — canonical OBS-talking-head recipe now git-backed at C:\Users\sidor\repos\video-use\docs\settings_reference.md (old M:/videos/OBS/edit-01/ path is a pointer); grade decision: bright→v7-contrast, dim→v7-lift, always 5-up sheet first
- [Reference — ffmpeg PATH](reference_ffmpeg_path.md) — ffmpeg/ffprobe installed (winget Gyan.FFmpeg) but tool shells' parent predates the PATH entry → "command not found"; prepend the winget bin dir to PATH in every ffmpeg-using Bash command
- [Reference — ElevenLabs isolation costs](reference_isolate_transcribe_helper.md) — helpers/isolate_and_transcribe.py DOES NOT EXIST (not in tree, not in git history); only the ElevenLabs cost/quota facts survive: Isolator ~661 cred/min, Scribe ~5x cheaper, per-key monthly cap is month-to-date
- [Feedback — render.py fps default](feedback_render_fps_default.md) — render.py extract defaults to 24fps; added a real `--fps` flag, pass `--fps 60` for OBS footage and verify r_frame_rate=60/1
- [Feedback — render.py concat apostrophe](feedback_render_concat_apostrophe.md) — concat_segments breaks on apostrophe in path ("couldn't"); fixed by escaping ' as '\'' in the concat list
- [Feedback — Scribe tail drift](feedback_scribe_tail_overshoot.md) — Scribe word.end on closing words ("bye.") drifts EITHER way (overshot 1.2s in edit-08, undershot 0.3s in edit-10); always read the waveform tail, never trust word.end
- [Feedback — Scribe onset token](feedback_scribe_onset_token.md) — phrase-onset tokens after long silences are degenerate (start sits in silence, e.g. "For" 112.22 vs real 113.31 in edit-11 → 1.25s dead gap); verify segment START with silencedetect, never raw word.start
- [Feedback — SRT stutter token](feedback_srt_stutter_token.md) — a stutter token ("f-") Scribe tags just inside a kept segment makes a phantom "F- AND" cue even when the audio has no f-; rebuild SRT from a filtered transcript COPY, never edit the cache
- [Feedback — SRT straddle dup](feedback_srt_straddle_dup.md) — splitting a take to trim an internal pause makes build_master_srt double-caption the word straddling the cut (phantom ~40ms cue); rebuild SRT with start-based membership (seg_start ≤ word.start < seg_end)
- [Feedback — trim pauses tight](feedback_trim_pauses_tight.md) — on this user's OBS monologues do NOT preserve 400-600ms breaths by merging phrases; trim every join to ~0.15s, verify with silencedetect — preserved pauses get flagged as defects
- [Reference — Windows UTF-8 helpers](reference_windows_utf8_helpers.md) — pack_transcripts/build_master_srt crash on cp1251 console printing →; run with PYTHONIOENCODING=utf-8 PYTHONUTF8=1 (files still written before the crash)
- [Feedback — SRT full script reconcile](feedback_srt_full_script_reconcile.md) — SRT from Scribe = what was HEARD, not the script; term-only spot-check is not enough (edit-26 shipped notation/Domain Specific/I already/that the/by human being wrong). Phase 5: diff EVERY cue word-by-word vs the user's prompt script, fix all in a transcript COPY (override text / merge for hyphens / insert missing article), rebuild; captions are a sidecar → no re-render
- [Feedback — silencedetect sub-word edges](feedback_silencedetect_subword_edges.md) — silence_start/end fire INSIDE quiet sibilant word tails (processes./notation./structure.) and before real onsets (pre-onset breaths); cross-check every edge vs word.end + a 40ms RMS probe before splitting/placing a cut; RMS ≳ −30dB in the "gap" = still speech, don't cut (edit-26); edit-31: never DISMISS a ≥0.3s gap as a sub-word tail without probing — probe every one at cut time
- [Feedback — pre-made isolated audio](feedback_premade_isolated_audio.md) — user may supply isolated.mp3 + transcript.json; don't re-run ElevenLabs — mux clean audio over video (-c copy) into source_clean.mp4 and cut from that
- [Feedback — script.txt reflects shipped ad-lib](feedback_script_txt_reflects_shipped_adlib.md) — when the only clean take adds an approved ad-lib not in the written script (edit-27 "Okay, maybe"), update script.txt to the SHIPPED wording BEFORE the SRT gate so it stays green; captions match audio, gate diffs against truth. Also the reverse (edit-31): script words never spoken — parenthetical glosses "(calls)", dropped articles, singular/plural — decide each mismatch by audio probe, fix script-side not transcript-side
- [Project — edit dir convention](project_edit_dir_convention.md) — every edit lives at M:/videos/OBS/prep/YYYY-MM/'YYYY-MM-DD[-NN] - <title> [REC YYYY-MM-DD]'/edit-NN/; month = SCRIPT month, title truncated to ~22 chars + '...', so resolve by date prefix not title; edit-NN no longer in month order; scan prep/*/*/edit-* recursively; global numbering, max = edit-45 as of 2026-08-31
- [Project — series → folder map](project_series_folder_map.md) — the English series names in the grade memories no longer match the Russian truncated folder titles; look a video's series up by edit-NN / script-date prefix
- [Project — data loss 2026-07](project_data_loss_2026_07.md) — system reinstall NUL-corrupted all session logs (unrecoverable, M: backup identical) + 4 memory files (restored 2026-07-13 from file-history); durable state → git + fork push, memory mirrored to repo .claude/memory-mirror/
- [Reference — ffmpeg filter loglevel](reference_ffmpeg_filter_loglevel.md) — silencedetect/astats/ebur128 print on ffmpeg INFO level; `-v error` HIDES all their output (looks like "no silence found") → invoke measurement filters with `-hide_banner` and NO `-v error` (edit-28 wasted rounds on this)
- [Feedback — complete-duplicate retake](feedback_complete_duplicate_retake.md) — a retake may be a FULL duplicate sentence with no truncation marker (edit-30 "I wish you no BSODs." ×2); spot it as a line repeated across the end/start of adjacent packed phrases, keep the copy that leaves the outro block continuous
- [Feedback — Keep Codex series grade](feedback_grade_keep_codex_series.md) — "Keep Codex Away from Your Computer" series (Part 1 = edit-28) default = v7-contrast on a DIM source (moody look, explicit user pick like edit-25); dim does NOT force lift; still measure + show the 5-up sheet
- [Feedback — Systems Analysis series grade](feedback_grade_systems_analysis_series.md) — the one series with NO settled grade: edit-26/27 → lift66/lift58 (v7-contrast crushed face to 22.7/36), edit-31/32 → v7-contrast (face held at ~47); never inherit, always measure + show the sheet
- [Feedback — spectral word-identity probe](feedback_word_identity_spectral_probe.md) — SRT-gate mismatches that are real voiced tokens (agent/engine, ran/run, a/the) get settled by band-energy probes vs control words from the SAME file, not by context or by trusting the script; edit-36 adds the manner-of-articulation read (a −45 dB stop closure kills a lateral) for mis-heard MULTI-WORD runs
- [Feedback — Scribe product-name mishears](feedback_scribe_product_name_mishears.md) — the same three brands break every video (Claude Code or Codex → Cloud Coder/Caller, Wispr Flow → Whisper, Glaido → Glider); expect them at the SRT gate, and note "Claude Code or" loses a token so it needs retiming, not a text override
- [Feedback — loudnorm shifts the silence threshold](feedback_loudnorm_shifts_silence_threshold.md) — Phase 6 gates run on final.mp4 AFTER loudnorm added ~+10dB; probing it at -35dB shows phantom "no silence" at real joins (edit-35: 3 of 11 missing) — shift both thresholds by (−14 − measured I), or gate final.prenorm.mp4
- [Feedback — join offsets from silencedetect](feedback_join_offsets_from_silencedetect.md) — Phase 6 onset probes must locate joins via silencedetect on final.mp4, never by summing EDL durations (re-encode rounding drifted ~0.13s by mid-file on edit-37 → two false "hot onset" flags); a real clip starts hot with no preceding decay
- [Feedback — silence_start lags speech end](feedback_silence_start_lags_speech_end.md) — silencedetect's silence_start can fire ~0.09s AFTER speech stops, so the +0.10/−0.06 pads leave 0.28s instead of the designed 0.16s (edit-40); verify every join width on base.mp4 and re-place edges ≥0.25s from an RMS probe
- [Feedback — final.mp4 AAC priming drift](feedback_final_mp4_aac_priming_drift.md) — plain PCM decode of final.mp4 runs LONG by ~21ms per segment (24 segs = +0.506s on edit-38); every Phase 6 probe drifts, floors read an impossible −10dB. Prefix `aresample=async=1000:first_pts=0` and verify decoded length == container duration. source_clean.mp4 has zero drift
- [Feedback — onset probe anchor artifact](feedback_onset_probe_anchor_artifact.md) — a probe anchored at `silence_end` is above the threshold BY DEFINITION, so "first 20ms > −25dB = clipped" false-fired on 18 of 23 clean joins (edit-38); judge the shape decay → −91dB splice notch → ramp, plus gap width vs the designed 0.16s
- [Feedback — raw audio noise floor](feedback_raw_audio_noise_floor.md) — no isolated.mp3 -> cut RAW by default (no isolation/de-noise tooling in the repo); measure floor F first and shift EVERY dB gate by D = F - (-55) (silencedetect -35, gap-RMS -30, onset -35/-25); durations never shift
- [Feedback — Scribe word.end swallows a pause](feedback_scribe_word_end_swallows_pause.md) — phrase-final word.end stretches through the following silence, so a word-gap scan emits NO candidate (edit-41 hid six 0.29–0.45s pauses that way, three visible only at −30dB); always intersect the silencedetect gap list with the kept ranges too
- [Feedback — join residual is fixed 0.16s](feedback_join_residual_is_fixed.md) — pads +0.10/−0.06 RETAIN 0.16s at every join regardless of gap width; never scale them, and never read a residual off EDL source-time arithmetic (that difference is the amount REMOVED) — measure it on base.mp4
- [Feedback — merge contiguous silence events](feedback_merge_contiguous_silence_events.md) — silencedetect fragments one dead-air join into back-to-back events; the nearest fragment reads 0.148s while the true span is 0.52s (edit-45) → coalesce events <0.03s apart BEFORE measuring any join residual; false PASS by construction
